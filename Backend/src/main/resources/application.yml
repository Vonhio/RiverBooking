server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/localhostdb?serverTimezone=UTC
    username: root
    password: Vonhio123

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
